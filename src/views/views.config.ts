import { Coordinates } from '../utils';

// ui control view
export const BALANCE_POSITION: Coordinates = {
  x: 150,
  y: 400
};
export const enum ButtonState {
  ACTIVE = 'active',
  UN_ACTIVE = 'un-active'
}
